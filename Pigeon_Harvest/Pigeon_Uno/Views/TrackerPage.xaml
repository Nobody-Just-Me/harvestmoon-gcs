<Page x:Class="Pigeon_Uno.Views.TrackerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Pigeon_Uno.Views"
      xmlns:converters="using:Pigeon_Uno.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
        <converters:BoolToVisConverter x:Key="BoolToVisConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Antenna Tracker" FontSize="24" FontWeight="Bold" Margin="20,20,0,0"/>
        
        <StackPanel Grid.Row="1" Spacing="20" Margin="20">
            <!-- Connection Panel -->
            <StackPanel Spacing="5">
                <TextBlock Text="Tracker Connection" FontWeight="Bold" FontSize="16"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ComboBox ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding TrackerPortName, Mode=TwoWay}" Width="150" PlaceholderText="Select Port"/>
                    <Button Command="{Binding RefreshPortsCommand}" Content="🔄"/>
                    <Button Command="{Binding ConnectTrackerCommand}" Content="Connect" Background="Green" Foreground="White"/> 
                    <Button Command="{Binding DisconnectTrackerCommand}" Content="Disconnect" Background="Red" Foreground="White"/>
                    <TextBlock Text="{Binding IsTrackerConnected}" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <!-- Tracker Pos -->
            <StackPanel Spacing="5">
                <TextBlock Text="Tracker Position (Home)" FontWeight="Bold" FontSize="16"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox Header="Latitude" Text="{Binding TrackerLat, Mode=TwoWay}" Width="150"/>
                    <TextBox Header="Longitude" Text="{Binding TrackerLon, Mode=TwoWay}" Width="150"/>
                    <TextBox Header="Altitude" Text="{Binding TrackerAlt, Mode=TwoWay}" Width="150"/>
                    <Button Command="{Binding SetHomeCommand}" Content="Set Current as Home" VerticalAlignment="Bottom" Margin="0,0,0,6"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Wahana Pos -->
            <StackPanel Spacing="5">
                <TextBlock Text="Vehicle Position" FontWeight="Bold" FontSize="16"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox Header="Latitude" Text="{Binding WahanaLat}" IsReadOnly="True" Width="150"/>
                    <TextBox Header="Longitude" Text="{Binding WahanaLon}" IsReadOnly="True" Width="150"/>
                    <TextBox Header="Altitude" Text="{Binding WahanaAlt}" IsReadOnly="True" Width="150"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Tracking Status -->
            <StackPanel Spacing="5">
                <TextBlock Text="Tracking Calculation" FontWeight="Bold" FontSize="16"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBox Header="Bearing" Text="{Binding Bearing, Converter={StaticResource StringFormatConverter}, ConverterParameter='F1'}" IsReadOnly="True" Width="150"/>
                    <TextBox Header="Pitch" Text="{Binding Pitch, Converter={StaticResource StringFormatConverter}, ConverterParameter='F1'}" IsReadOnly="True" Width="150"/>
                    <TextBox Header="Distance" Text="{Binding Distance, Converter={StaticResource StringFormatConverter}, ConverterParameter='F1'}" IsReadOnly="True" Width="150"/>
                    <ToggleButton IsChecked="{Binding IsTracking, Mode=TwoWay}" Content="Tracking Active" VerticalAlignment="Bottom" Margin="0,0,0,6"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
