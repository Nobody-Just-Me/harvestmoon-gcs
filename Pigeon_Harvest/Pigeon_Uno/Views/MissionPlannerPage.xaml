<Page
    x:Class="Pigeon_Uno.Views.MissionPlannerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Pigeon_Uno.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource BackgroundBrush}">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
        </Style>

        <Style x:Key="OutlineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{ThemeResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource OutlineButtonStyle}">
            <Setter Property="Background" Value="{ThemeResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{ThemeResource PrimaryForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource PrimaryBrush}"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20,16,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <TextBlock Text="Mission Planner" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource ForegroundBrush}"/>
                <Border Background="#1F4CAF50" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="999" Padding="10,4">
                    <TextBlock Text="Map Routing Active" Foreground="{ThemeResource ForegroundBrush}" FontWeight="SemiBold" FontSize="12"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button x:Name="AddWaypointButton" Content="Add WP" Click="AddWaypointButton_Click" Style="{StaticResource OutlineButtonStyle}"/>
                <Button x:Name="ClearWaypointButton" Content="Clear" Click="ClearWaypointButton_Click" Style="{StaticResource OutlineButtonStyle}"/>
                <Button x:Name="UploadMissionButton" Content="Upload Mission" Click="UploadMissionButton_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,10" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Map Provider" VerticalAlignment="Center" Foreground="{ThemeResource MutedForegroundBrush}"/>
                        <ComboBox x:Name="MapProviderComboBox" Grid.Column="1" Width="180" SelectionChanged="MapProviderComboBox_SelectionChanged">
                            <ComboBoxItem Content="ArcGIS Topographic"/>
                            <ComboBoxItem Content="ArcGIS Imagery"/>
                            <ComboBoxItem Content="ArcGIS StreetMap"/>
                            <ComboBoxItem Content="Google Map"/>
                            <ComboBoxItem Content="Google Satellite"/>
                            <ComboBoxItem Content="Google Terrain"/>
                            <ComboBoxItem Content="Google Hybrid"/>
                        </ComboBox>

                        <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <TextBlock Text="Geofence" VerticalAlignment="Center" Foreground="{ThemeResource MutedForegroundBrush}"/>
                            <Slider x:Name="GeofenceRadiusSlider" Width="120" Minimum="50" Maximum="500" Value="180" ValueChanged="GeofenceRadiusSlider_ValueChanged"/>
                            <TextBlock x:Name="GeofenceLabel" Text="180 m" VerticalAlignment="Center" Foreground="{ThemeResource ForegroundBrush}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>

                    <controls:SkiaMapControl x:Name="MissionMapControl"
                                             Grid.Row="1"
                                             DoubleTapped="MissionMapControl_DoubleTapped" />
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Waypoint Queue" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource ForegroundBrush}" Margin="0,0,0,10"/>

                    <ListView x:Name="WaypointListView" Grid.Row="1" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Background="#1F2E7D32" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="999" Padding="8,2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Sequence}" Foreground="{ThemeResource ForegroundBrush}" FontWeight="SemiBold"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Spacing="2">
                                        <TextBlock Text="{Binding Coordinates}" Foreground="{ThemeResource ForegroundBrush}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding AltitudeText}" Foreground="{ThemeResource MutedForegroundBrush}" FontSize="12"/>
                                    </StackPanel>

                                    <Button Grid.Column="2"
                                            Content="✕"
                                            Tag="{Binding Sequence}"
                                            Click="RemoveWaypointButton_Click"
                                            Style="{StaticResource OutlineButtonStyle}"
                                            Padding="8,2"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Border Grid.Row="2" Margin="0,10,0,0" Padding="10" CornerRadius="8" Background="#151A2530" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1">
                        <TextBlock x:Name="WaypointSummaryText" Text="0 waypoints" Foreground="{ThemeResource ForegroundBrush}" FontWeight="SemiBold"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
