<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Merge Colors -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Font Families -->
    <FontFamily x:Key="RalewayFont">ms-appx:///Assets/fonts/Raleway/Raleway-Regular.ttf#Raleway</FontFamily>
    <FontFamily x:Key="RalewayBoldFont">ms-appx:///Assets/fonts/Raleway/Raleway-Bold.ttf#Raleway</FontFamily>
    <FontFamily x:Key="RalewaySemiBoldFont">ms-appx:///Assets/fonts/Raleway/Raleway-SemiBold.ttf#Raleway</FontFamily>
    <FontFamily x:Key="RalewayLightFont">ms-appx:///Assets/fonts/Raleway/Raleway-Light.ttf#Raleway</FontFamily>
    <FontFamily x:Key="CommandoFont">ms-appx:///Assets/fonts/commando.ttf#Commando</FontFamily>

    <!-- Ensure glyph-based icons render consistently on Windows, Android, and Linux -->
    <Style TargetType="FontIcon">
        <Setter Property="FontFamily" Value="{ThemeResource SymbolThemeFontFamily}"/>
    </Style>

    <!-- Button Styles -->
    <Style x:Key="LeftPaneButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Height" Value="80"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <CompositeTransform/>
            </Setter.Value>
        </Setter>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource ButtonBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <CompositeTransform/>
            </Setter.Value>
        </Setter>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource PrimaryColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewaySemiBoldFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <CompositeTransform/>
            </Setter.Value>
        </Setter>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <CompositeTransform/>
            </Setter.Value>
        </Setter>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Animated Button Style with Hover Effects -->
    <Style x:Key="AnimatedButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource ButtonBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <CompositeTransform/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid" Background="{TemplateBinding Background}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="0.9"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="1.0"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ContentPresenter"
                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)"
                                                       To="1.05"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ContentPresenter"
                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)"
                                                       To="1.05"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ContentPresenter"
                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)"
                                                       To="0.95"
                                                       Duration="0:0:0.1">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseIn"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="ContentPresenter"
                                                       Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)"
                                                       To="0.95"
                                                       Duration="0:0:0.1">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseIn"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="0.5"
                                                       Duration="0:0:0.2"/>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                            <ContentPresenter x:Name="ContentPresenter"
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Padding="{TemplateBinding Padding}"
                                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                            RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter.RenderTransform>
                                    <CompositeTransform/>
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBlock Styles -->
    <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource RalewayBoldFont}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
    </Style>

    <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource RalewaySemiBoldFont}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
    </Style>

    <Style x:Key="BodyTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
    </Style>

    <Style x:Key="CaptionTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{ThemeResource SecondaryTextColor}"/>
    </Style>

    <Style x:Key="LabelTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource RalewaySemiBoldFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>

    <!-- TextBox Styles -->
    <Style x:Key="StandardTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
    </Style>

    <!-- ComboBox Styles -->
    <Style x:Key="StandardComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
    </Style>

    <!-- Border Styles -->
    <Style x:Key="PanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource PanelBackgroundColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource LightBackgroundColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="4"/>
    </Style>

    <!-- ProgressBar Styles -->
    <Style x:Key="StandardProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryColor}"/>
        <Setter Property="Height" Value="8"/>
        <Setter Property="CornerRadius" Value="4"/>
    </Style>

    <!-- Slider Styles -->
    <Style x:Key="StandardSliderStyle" TargetType="Slider">
        <Setter Property="Foreground" Value="{ThemeResource PrimaryColor}"/>
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
    </Style>

    <!-- CheckBox Styles -->
    <Style x:Key="StandardCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- RadioButton Styles -->
    <Style x:Key="StandardRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- ToggleButton Styles -->
    <Style x:Key="StandardToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="{ThemeResource ButtonBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="FontFamily" Value="{StaticResource RalewayFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="4"/>
    </Style>

    <!-- ListView Styles -->
    <Style x:Key="StandardListViewStyle" TargetType="ListView">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Grid Styles -->
    <Style x:Key="DataGridStyle" TargetType="Grid">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
    </Style>

    <!-- Status Indicator Styles -->
    <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="Margin" Value="4"/>
    </Style>

    <!-- Loading Indicator Style -->
    <Style x:Key="LoadingIndicatorStyle" TargetType="ProgressRing">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Foreground" Value="{ThemeResource PrimaryColor}"/>
        <Setter Property="IsActive" Value="True"/>
    </Style>

    <!-- Connection Status Style -->
    <Style x:Key="ConnectionStatusStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource DarkBackgroundColor}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <EntranceThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Notification Banner Style -->
    <Style x:Key="NotificationBannerStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource PrimaryColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <EntranceThemeTransition FromVerticalOffset="-50"/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Error Banner Style -->
    <Style x:Key="ErrorBannerStyle" TargetType="Border" BasedOn="{StaticResource NotificationBannerStyle}">
        <Setter Property="Background" Value="#D32F2F"/>
    </Style>

    <!-- Success Banner Style -->
    <Style x:Key="SuccessBannerStyle" TargetType="Border" BasedOn="{StaticResource NotificationBannerStyle}">
        <Setter Property="Background" Value="#388E3C"/>
    </Style>

    <!-- Warning Banner Style -->
    <Style x:Key="WarningBannerStyle" TargetType="Border" BasedOn="{StaticResource NotificationBannerStyle}">
        <Setter Property="Background" Value="#F57C00"/>
    </Style>

</ResourceDictionary>
