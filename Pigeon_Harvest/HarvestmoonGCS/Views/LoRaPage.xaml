<Page x:Class="HarvestmoonGCS.Views.LoRaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:HarvestmoonGCS.Views"
      xmlns:converters="using:HarvestmoonGCS.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToConnectTextConverter x:Key="BoolToConnectTextConverter"/>
        <converters:BoolToConnectColorConverter x:Key="BoolToConnectColorConverter"/>
    </Page.Resources>

        <Grid Background="#FF1a1a2e">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF16213e" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="📡 LoRa Relay Network" FontSize="28" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                    <ComboBox Width="150" ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedPort, Mode=TwoWay}" PlaceholderText="Select Port"/>
                    <Button Content="{Binding IsConnected, Converter={StaticResource BoolToConnectTextConverter}}" 
                            Command="{Binding ToggleConnectionCommand}"
                            Background="{Binding IsConnected, Converter={StaticResource BoolToConnectColorConverter}}" 
                            Foreground="White"/>
                    <Button Content="Scan" Command="{Binding ScanDevicesCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Terminal -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

                <TextBox Text="{Binding EventLog, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" 
                     FontFamily="Consolas" Background="#FF1f1f3d" Foreground="LimeGreen" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>

            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CommandInputBox" PlaceholderText="Enter AT command..." KeyDown="CommandInput_KeyDown"/>
                <Button Grid.Column="1" Content="Send" Click="SendCommand_Click" Margin="10,0,0,0" Background="#FF0f4c75" Foreground="White"/>
            </Grid>
            <Border Grid.RowSpan="2" Background="#FF1a1a2e"/>
        </Grid>
    </Grid>
</Page>
