<Page x:Class="HarvestmoonGCS.Views.CalibrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:HarvestmoonGCS.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <NavigationView PaneDisplayMode="Left" IsSettingsVisible="False" Header="Calibration" SelectionChanged="NavigationView_SelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem Content="Accelerometer" Tag="Accel" IsSelected="True"/>
            <NavigationViewItem Content="Compass" Tag="Compass"/>
            <NavigationViewItem Content="Motor Test" Tag="MotorTest"/>
        </NavigationView.MenuItems>

        <Frame x:Name="CalibrationContentFrame">
            <!-- Initial content will be set in code-behind or handled via visibility -->
            <!-- For simplicity in single-page design, we can use a Grid with Visibility binding if not navigating to separate pages -->
            <Grid Margin="20">
                <!-- ACCEL VIEW -->
                <StackPanel x:Name="AccelPanel" Spacing="20" Visibility="Visible">
                    <TextBlock Text="Accelerometer Calibration" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Place vehicle level, then on each side as prompted." Foreground="Gray"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Start Calibration" Command="{Binding StartAccelCalibrationCommand}" Background="Green" Foreground="White"/>
                        <Button Content="Calibrate Level Only" Command="{Binding StartLevelCalibrationCommand}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding AccelStatus}" FontSize="16" Foreground="Cyan"/>
                    <TextBlock Text="{Binding CalibrationStatus}" FontSize="14" Foreground="Yellow" Margin="0,20,0,0"/>
                </StackPanel>

                <!-- COMPASS VIEW -->
                <StackPanel x:Name="CompassPanel" Spacing="20" Visibility="Collapsed">
                    <TextBlock Text="Compass Calibration" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Click Start, then rotate vehicle in all axes until progress completes." Foreground="Gray"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Start" Command="{Binding StartCompassCalibrationCommand}" Background="Green" Foreground="White"/>
                        <Button Content="Cancel" Command="{Binding CancelCompassCalibrationCommand}" Background="Red" Foreground="White"/>
                    </StackPanel>
                    <ProgressBar Value="{Binding CompassProgress}" Maximum="100" Height="20"/>
                    <TextBlock Text="{Binding CompassStatus}" FontSize="16" Foreground="Cyan"/>
                    <TextBlock Text="{Binding CalibrationStatus}" FontSize="14" Foreground="Yellow" Margin="0,20,0,0"/>
                </StackPanel>

                <!-- RADIO VIEW -->
                <StackPanel x:Name="RadioPanel" Spacing="20" Visibility="Collapsed">
                    <TextBlock Text="Radio Calibration" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Move all sticks to extents." Foreground="Gray"/>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="Ch 1"/>
                        <ProgressBar Grid.Row="0" Grid.Column="1" Minimum="1000" Maximum="2000" Value="{Binding Ch1, Mode=OneWay}"/>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Ch1}"/>

                        <TextBlock Grid.Row="1" Text="Ch 2"/>
                        <ProgressBar Grid.Row="1" Grid.Column="1" Minimum="1000" Maximum="2000" Value="{Binding Ch2, Mode=OneWay}"/>
                        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding Ch2}"/>

                        <TextBlock Grid.Row="2" Text="Ch 3"/>
                        <ProgressBar Grid.Row="2" Grid.Column="1" Minimum="1000" Maximum="2000" Value="{Binding Ch3, Mode=OneWay}"/>
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Ch3}"/>

                        <TextBlock Grid.Row="3" Text="Ch 4"/>
                        <ProgressBar Grid.Row="3" Grid.Column="1" Minimum="1000" Maximum="2000" Value="{Binding Ch4, Mode=OneWay}"/>
                        <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding Ch4}"/>
                        
                        <!-- More channels skipped for brevity, but pattern is clear -->
                    </Grid>
                </StackPanel>

                <!-- FLIGHT MODES VIEW -->
                <StackPanel x:Name="FlightModesPanel" Spacing="20" Visibility="Collapsed">
                    <TextBlock Text="Flight Modes" FontSize="24" FontWeight="Bold"/>
                    <StackPanel Spacing="10">
                        <ComboBox Header="Mode 1" SelectedIndex="{Binding FlightMode1, Mode=TwoWay}" Width="200">
                            <ComboBoxItem Content="Stabilize"/>
                            <ComboBoxItem Content="Loiter"/>
                            <ComboBoxItem Content="RTL"/>
                            <!-- Add more items as needed -->
                        </ComboBox>
                        <!-- Add more combos for Mode 2-6 -->
                        <Button Content="Save Modes" Command="{Binding SaveFlightModesCommand}"/>
                    </StackPanel>
                </StackPanel>

                <!-- SERVO OUTPUT VIEW -->
                <StackPanel x:Name="ServoOutputPanel" Spacing="20" Visibility="Collapsed">
                    <TextBlock Text="Servo Output" FontSize="24" FontWeight="Bold"/>
                    <ListView ItemsSource="{Binding ServoOutputs}" Height="400" BorderBrush="Gray" BorderThickness="1">
                        <ListView.Header>
                            <Grid Padding="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="No" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1" Text="Rev" FontWeight="Bold"/>
                                <TextBlock Grid.Column="2" Text="Func" FontWeight="Bold"/>
                                <TextBlock Grid.Column="3" Text="Output" FontWeight="Bold"/>
                                <TextBlock Grid.Column="4" Text="Min" FontWeight="Bold"/>
                                <TextBlock Grid.Column="5" Text="Trim" FontWeight="Bold"/>
                                <TextBlock Grid.Column="6" Text="Max" FontWeight="Bold"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Channel}" VerticalAlignment="Center"/>
                                    <CheckBox Grid.Column="1" IsChecked="{Binding Reverse, Mode=TwoWay}" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="2" Text="{Binding Function, Mode=TwoWay}" Margin="2"/>
                                    <ProgressBar Grid.Column="3" Minimum="1000" Maximum="2000" Value="{Binding OutputPwm, Mode=OneWay}" Height="15" Margin="5"/>
                                    <TextBox Grid.Column="4" Text="{Binding Min, Mode=TwoWay}" Margin="2"/>
                                    <TextBox Grid.Column="5" Text="{Binding Trim, Mode=TwoWay}" Margin="2"/>
                                    <TextBox Grid.Column="6" Text="{Binding Max, Mode=TwoWay}" Margin="2"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <!-- MOTOR TEST VIEW -->
                <StackPanel x:Name="MotorPanel" Spacing="20" Visibility="Collapsed">
                    <TextBlock Text="Motor Test" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="WARNING: Props should be removed! Motors will spin." Foreground="Red" FontWeight="Bold"/>
                    
                    <Slider Header="Throttle (%)" Minimum="0" Maximum="100" Value="{Binding MotorTestThrottle, Mode=TwoWay}" Width="300" HorizontalAlignment="Left"/>
                    <Slider Header="Duration (Sec)" Minimum="1" Maximum="10" Value="{Binding MotorTestDuration, Mode=TwoWay}" Width="300" HorizontalAlignment="Left"/>
                    
                    <TextBlock Text="Select Motor to Test:" Margin="0,10,0,0"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Motor 1" Command="{Binding TestMotorCommand}" CommandParameter="1"/>
                        <Button Content="Motor 2" Command="{Binding TestMotorCommand}" CommandParameter="2"/>
                        <Button Content="Motor 3" Command="{Binding TestMotorCommand}" CommandParameter="3"/>
                        <Button Content="Motor 4" Command="{Binding TestMotorCommand}" CommandParameter="4"/>
                    </StackPanel>
                    <TextBlock Text="{Binding CalibrationStatus}" FontSize="14" Foreground="Yellow" Margin="0,20,0,0"/>
                </StackPanel>
            </Grid>
        </Frame>
    </NavigationView>
</Page>
